import React from 'react';

import localization from '../../localization/localization';

const Main = () => (
    <div className="Main">
        <span className="Text">
            {localization.main1}
        </span>
    </div>
);

export default Main;
